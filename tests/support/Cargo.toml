[package]
name = "telekio-test-support"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
test = false
doctest = false
bench = false

[dependencies]
libloading = "0.9"
telekio-abi = { path = "../../crates/telekio-abi", features = ["guest"] }
