[package]
name = "upstream-tests"
version = "0.1.0"
edition = "2024"
publish = false

[features]
std-lock = []

[build-dependencies]
telekio-build = { version = "0.1.0", path = "../../crates/telekio-build" }
toml_edit = "0.25.13"
